class Singers():
    """docstring for ClassName"""
    def __init__(self, skintone, gender, vocalrange, hairlength, musicstyle, age, name):
        self.skintone = skintone
        self.gender = gender 
        self.gender = vocalrange 
        self.vocalrange = vocalrange 
        self.hairlength = hairlength
        self.musicstyle = musicstyle 
        self.age = age
        self.name = name 
        self.pronoun = 'they'

        #Method
        def pronouns(): 
            if (self.gender == 'female'):
                return self.pronoun = 'she'
            elif (self.gender == 'male'):
                return self.prounon = 'he'
            return self.prounoun 
        def Singing(song): 
            print("The famous " + self.name + " sings " + song + " in the range of " + self.vocalRange)

        def Dancing:
            print( " Whoa, " + self.pronoun() + " dances better than Beyonce. That's cray cray"

 erica = Singers
 







#attr: skin tone, gender, vocal range, hair length,
        #Beyonce, Ariana, Justin Beiber, Zayn